import { FeedPage } from './feed-page';

describe('FeedPage', () => {
  let component: FeedPage;

  beforeEach(async () => {
    component = new FeedPage();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
